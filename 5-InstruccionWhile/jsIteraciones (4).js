function mostrar()
{

	var numero = prompt("ingrese un número entre 0 y 10.");
	var Validacion
	numero = parseInt(numero)
	Validacion = Numero.value
		while(numero <10)
		{numero=Numero.value
			numero++}
	
}//FIN DE LA FUNCIÓN
//al presionar el botón pedir un número entre 0 y 9 inclusive.
